<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Profile</title>
    <link href="https://fonts.googleapis.com/css2?family=Nunito:wght@200;300;400;600;700&display=swap"
        rel="stylesheet" />
    <style>
        :root {
            --primary-color: #ffa500;
            --secondary-color: #333;
            --background-color: #f3f4f6;
            --text-color: #555;
            --white-color: #fff;
            --content-padding: 20px; /* Khoảng cách cho .content */
            --footer-height: 80px; /* Chiều cao của footer */
        }

        body {
            font-family: "Nunito", sans-serif;
            margin: 0;
            display: flex;
            flex-direction: column;
            min-height: 100vh; /* Đảm bảo body chiếm toàn bộ chiều cao màn hình */
            background-color: var(--background-color);
        }

        .content {
            display: flex;
            justify-content: center;
            align-items: center;
            flex-grow: 1; /* Giúp content chiếm không gian còn lại giữa header và footer */
            padding: var(--content-padding);
        }

        .container {
            width: 100%;
            max-width: 350px;
            padding: 20px 30px;
            background-color: var(--white-color);
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
            border-radius: 8px;
            text-align: center;
            margin-top: 80px;
        }

        .container h2 {
            font-weight: 600;
            margin-bottom: 20px;
            font-size: 24px;
            color: var(--secondary-color);
        }

        .container .profile-info {
            font-size: 16px;
            margin-bottom: 20px;
        }

        .container .profile-info p {
            margin: 10px 0;
        }

        .container button {
            width: 100%;
            padding: 10px;
            background-color: var(--primary-color);
            border: none;
            color: var(--white-color);
            font-size: 16px;
            font-weight: 600;
            border-radius: 4px;
            cursor: pointer;
            margin-top: 10px;
        }

        .container button:hover {
            background-color: darkorange;
        }

        .container a {
            color: var(--primary-color);
            text-decoration: none;
        }

        .container p {
            font-size: 14px;
            color: var(--text-color);
        }
    </style>
</head>

<body>
    <div class="content">
        <div class="container">
            <h2>Profile</h2>

            <!-- Hiển thị thông tin của người dùng -->
            <div class="profile-info">
                <p><strong>Username:</strong> {{user.username}}</p>
                <p><strong>Email:</strong> {{user.email}}</p>
                <p><strong>Phone Number:</strong> {{user.phoneNumber}}</p>
                <p><strong>Address:</strong> {{user.address}}</p>
            </div>

            <!-- Nút để logout -->
            <button onclick="window.location.href='/v1/auth/logout'">Logout</button>
        </div>
    </div>
</body>

</html>
